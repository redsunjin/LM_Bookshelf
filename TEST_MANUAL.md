# LM Bookshelf 테스트 매뉴얼

## 0. 사전 준비

1. `chrome://extensions/` 열기
2. `개발자 모드` 활성화
3. `압축해제된 확장 프로그램을 로드합니다` 클릭
4. [`src`](/c:/Users/sunji/workspace/Browser-Extension-Framework/LM_Bookshelf/src) 선택

## 1. 빠른 저장 팝업 테스트

1. `NotebookLM` 페이지 또는 일반 페이지를 연다.
2. 익스텐션 아이콘을 클릭한다.
3. 현재 탭의 제목과 URL이 자동 반영되는지 확인한다.
4. 폴더, 태그, 메모를 입력하고 저장한다.
5. 최근 항목 리스트에 저장한 항목이 나타나는지 확인한다.

## 2. 검색 및 재사용 테스트

1. 팝업 하단 검색창에 제목 일부를 입력한다.
2. 필터된 결과만 보이는지 확인한다.
3. 최근 항목의 `불러오기` 버튼을 눌러 입력 폼이 다시 채워지는지 확인한다.

## 3. 관리자 화면 테스트

1. 팝업에서 `매니저` 버튼을 누른다.
2. 항목 수와 폴더 수가 정상 표시되는지 확인한다.
3. 검색어, 폴더 필터, 태그 필터가 각각 작동하는지 확인한다.
4. 항목 수정 후 저장했을 때 목록에 반영되는지 확인한다.
5. 항목 삭제 후 목록에서 사라지는지 확인한다.

## 4. 스마트 태그 테스트

1. 제목이나 메모에 `research`, `meeting`, `roadmap`, `policy` 같은 키워드를 입력한다.
2. 스마트 제안 영역에 관련 태그가 나타나는지 확인한다.
3. 제안 태그를 눌렀을 때 태그 입력값에 반영되는지 확인한다.

## 5. 백업/복원 테스트

1. 관리자 화면에서 `JSON 내보내기`를 눌러 파일을 저장한다.
2. 테스트 항목 하나를 삭제한다.
3. `JSON 가져오기`로 방금 저장한 파일을 다시 불러온다.
4. 삭제했던 항목이 복원되는지 확인한다.

## 6. 체크리스트

- [ ] 팝업이 열릴 때 오류가 없다.
- [ ] 저장 후 최근 항목에 즉시 반영된다.
- [ ] 관리자 화면과 팝업 사이 상태가 동기화된다.
- [ ] JSON 백업/복원이 정상 작동한다.
- [ ] `NotebookLM` 링크가 아닌 경우에도 수동 저장이 가능하다.
